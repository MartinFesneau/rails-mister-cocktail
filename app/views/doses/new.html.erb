<div class="container mt-5 ">
  <%= link_to 'Back to cocktails list', cocktail_path(@cocktail), class: 'btn-sncf' %>
  <hr>
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient %>
    <%= f.submit class: 'btn-sncf'%>
  <% end %>
</div>